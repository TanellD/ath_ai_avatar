# Описание проекта — промежуточная версия

Цифровой тренажёр корпоративных тренировок с говорящим аватаром.

## Аннотация

Веб-приложение, в котором сотрудник проходит тренировочный диалог с говорящим
3D-персонажем по заданному сценарию: собеседование, продажа, работа с
возражением, аттестация. Методист настраивает сценарий и критерии оценки, а
после прохождения получает историю разговора и итоговую оценку, где под каждым
баллом стоит дословная цитата из реплики сотрудника.

Ключевое архитектурное решение — рендер персонажа в браузере вместо облачного
видео-аватара. Из него следуют задержка до первого звука порядка секунды,
остановка речи при перебивании за десятки миллисекунд и себестоимость минуты
на порядок ниже, чем у готовых видео-сервисов.

Голосовой ввод, изначально запланированный отдельным этапом, реализован:
захват микрофона, push-to-talk с локальным детектором паузы, потоковый STT с
автоматическим переключением провайдера (Soniox → GigaAM). Текстовый ввод
остаётся рабочим — оба пути идут через один и тот же протокол отмены.

Сквозной прототип собран, часть метрик уже измерена на синтетических
прогонах (`docs/PRODUCT_demo_technical_metrics.md`) — числа ниже, где не
указано иное, по-прежнему расчётные оценки и помечены соответственно.

## Проблематика

Корпоративные тренировки — собеседования, продажи, работа с возражениями,
проверка знаний — сегодня проводятся вручную. Тренер готовит сценарий, лично
ведёт диалог с каждым участником, разбирает ответы и формирует оценку.

Одна тренировка вместе с подготовкой и разбором занимает около двух часов. При
ставке специалиста 1 500 ₽/час и ста тренировках в месяц это около
**300 000 ₽ ежемесячно**.

Боль распределена по трём сторонам:

- **Бизнес** платит за механическую работу человека.
- **Методист** выгорает на сотом прогоне одного и того же базового диалога;
  рутина вытесняет разработку методики.
- **Сотрудник** испытывает стресс при симуляции с руководителем и не может
  тренироваться вечером из дома, ошибаясь без последствий.

## Постановка задачи

Передать проведение типового диалога ИИ-агенту с говорящим аватаром, оставив
методисту определение сценария и критериев оценки.

Требуется веб-приложение, в котором реализованы:

1. выбор или настройка сценария тренировки;
2. диалог с агентом, учитывающим предыдущие ответы и состояние сценария;
3. поэтапное озвучивание реплик через TTS без ожидания полного ответа;
4. говорящий аватар с мимикой, синхронной фактически воспроизводимому аудио;
5. субтитры;
6. прерывание текущей речи и анимации новой репликой пользователя;
7. гарантированное отбрасывание устаревшего ответа;
8. формирование итогового отчёта.

Приёмочные пороги: первый звук ≤ 3 с, рассинхрон губ ≤ 200 мс, остановка при
перебивании ≤ 300 мс, возвраты отменённого хвоста ровно 0, корректное
прохождение минимум 4 из 5 проверочных диалогов.

## Выбранное техническое решение

### Главный размен

Отказ от облачного видео-аватара (HeyGen, Simli, D-ID) в пользу рендера
3D-персонажа в браузере — TalkingHead + HeadAudio (MIT, Mika Suominen). Сервер
отдаёт только текст и аудио; мимика считается на GPU пользователя.

| Критерий | Облачный видео-аватар | Наше решение |
|---|---|---|
| Задержка до первого кадра | 1.5–4 с | ~0, модель уже в браузере |
| Прерывание | Сброс видеопотока, возможен рассинхрон | Локальная остановка < 25 мс, без сети |
| Устойчивость к сети | Губы «плывут» при джиттере | Аудио и мимика синхронны локально |
| Стоимость | $0.01–0.05 за минуту | Только LLM и TTS |
| Нагрузка на сервер | GPU на каждую сессию | По графике сервер не нагружен |

Цена размена — стилизованный персонаж вместо фотореалистичного. Кейс выносит
фотореализм за скоуп, поэтому мы обменяли реализм на реактивность и
себестоимость. Побочный эффект: не используются голос и изображение реального
человека, вопрос согласия снимается по построению.

Решение подтверждено рабочим спайком в ветке `poc` до переноса в основную
архитектуру.

### Состав системы

Шесть сервисов, поднимаются одной командой `docker compose up --build`
(`gigaam-worker` — за отдельным профилем `--profile gigaam`, по умолчанию не
поднимается).

| Сервис | Порт | Владеет |
|---|---|---|
| `frontend` | 5173 | Воспроизведение, часы, липсинк, субтитры, экраны обеих ролей, микрофон |
| `gateway` | 8000 | `gen_id`, автомат этапов, окно контекста, персистентность, голосовые ходы |
| `speech-service` | 8010 | TTS (Soniox) и realtime STT (Soniox / GigaAM с failover) |
| `gigaam-worker` | 8020 | Опциональный self-hosted STT-воркер (GigaAM, CPU, офлайн после предзагрузки весов) |
| `ai-service` | 8030 | Реплики персонажа, классификация, итоговая оценка |
| `scenario-service` | 8050 | Сценарии, шаблоны, рубрики; редактор — `ScenarioEditor.tsx` |

Транспорты: браузер ⇄ gateway — WebSocket; gateway ⇄ speech — WebSocket;
gateway ⇄ ai — HTTP + SSE; gateway ⇄ scenario — HTTP.

`packages/contracts/` — устанавливаемый пакет с моделями данных, единственный
источник истины; рассинхрон между сервисами ловится импортом, а не
соглашением.

Провайдеры: LLM — Anthropic, OpenAI-совместимый прокси или локальный
Ollama-сервер (см. ниже, «Переход на локальную модель»); TTS/STT — Soniox,
STT дополнительно GigaAM локально. По умолчанию обе заглушки `mock`, поэтому
система поднимается и проходит health-check без единого ключа.

### Переход на локальную модель

Реплика персонажа — единственное место, где задержка напрямую видна
собеседнику (порог 3 с, цель 1.5 с). Общий прокси-шлюз команды не давал
стабильно укладываться в этот бюджет и не позволял управлять стоимостью
токена, поэтому для роли «реплика персонажа» перешли на собственный
Ollama-сервер с локальными открытыми моделями — цена токена контролируема
(вплоть до нуля сверх амортизации железа, см. `PRODUCT_hypotheses.md`, Г5), а
задержка не зависит от чужого прокси.

**Первая находка — скрытый reasoning.** Модели семейства Qwen3 по умолчанию
генерируют цепочку рассуждений в отдельном поле ответа *до* самого текста
реплики. Наш стрим читает только содержательное поле, поэтому «размышления»
не протекали в субтитры, но съедали весь бюджет до первого токена и иногда
исчерпывали `max_tokens` раньше, чем модель доходила до собственно ответа.
Решение — `reasoning_effort: "none"` в теле запроса (поддерживается Ollama
0.32+), полностью отключающее трейс: без него TTFT доходил до 6–40 с на
холодном старте, с ним — стабильно ~0.6 с на прогретой модели.

**Сравнение моделей.** Замерены TTFT и скорость декодирования (ток/с) на
развёрнутом контрольном промпте (не короткой фразе, где throughput не
успевает проявиться) по тринадцати доступным локальным моделям — от
`qwen3:14b` (плотная, 14B) до нескольких MoE-моделей на 26–35B. Ключевой
результат — размер модели не предсказывает скорость декодирования:

| Модель | Архитектура | ток/с (устойчиво) |
|---|---|---:|
| `aratan/qwen3.7-35b-q4` | MoE, 35B | 264 |
| `nemotron-3-nano` | MoE | 246 (холодный старт 61 с — неприемлемо для интерактива) |
| `qwen3.6:35b` | MoE, 35B | 225 |
| `gemma4:26b-a4b-it` | MoE | 212 |
| `qwen3:14b-q4_K_M` | плотная, 14B | 145 |
| `huihui_ai/Qwen3.8-abliterated:27b` | плотная, 27B, без цензуры | 83 |
| `qwen3.8:27b-q4_K_M` | плотная, 27B | 80 |
| `gemma4:31b-it` | плотная, 31B | 71 |

MoE-модели на 35B оказались **быстрее** плотной 14B (225–264 против 145
ток/с) — на каждый токен декодирования у них реально работает лишь часть
параметров. Отдельно проверена гипотеза «для резкого/грубого персонажа нужна
модель без цензуры»: A/B-сравнение `huihui_ai` (abliterated) против
`qwen3.6:35b` на максимально враждебном системном промпте не показало
разницы в готовности говорить резко и в характере — обе одинаково
оставались в роли без отказов. Абляция при этом стоит производительности
(83 против 225 ток/с), поэтому от неё отказались.

**Итоговый выбор — `qwen3.6:35b`** на обе роли (реплика и итоговая оценка):
лучшее сочетание устойчивого TTFT (~0.6 с прогретая), throughput (225 ток/с)
и качества среди проверенных вариантов; одна модель на обе роли — меньше
операционной сложности, чем раздельные модели без измеримого выигрыша в
качестве. Себестоимость минуты с этой моделью посчитана дважды —
расчётно и на живом 5-минутном прогоне через полный стек (`gateway` →
`ai-service` → Ollama): 51 121 токен за 6.2 минуты сессии, что при
пересчёте на тариф Claude Sonnet 5 дало бы ≈ 2.4 ₽/мин — на своей
инфраструктуре стоимость токена нулевая сверх амортизации.

### Три решения, на которых держится приёмка

**Переход между этапами делает код, а не модель.** LLM классифицирует ответ
(`complete | incomplete | off_topic`), переход выполняет автомат в
`gateway/app/orchestrator/fsm.py`. Свободная навигация модели даёт
преждевременные переходы и нарушает методику заказчика.

**Часы системы — фактически воспроизводимое аудио.** HeadAudio анализирует
реально проходящие сэмплы через AudioWorklet и толкает значения визем прямо в
морф-таргеты меша; отдельного опроса нет вообще. Требование закреплено
механически: ESLint запрещает `setInterval` и `setTimeout` в `src/audio/**` и
`src/avatar/**`.

**Отмена — инвариант, а не гонка таймаутов.** Каждый ответ несёт монотонный
`gen_id`, проставленный во все производные артефакты. Проверка стоит с двух
сторон независимо: `GenerationRegistry.is_stale` на сервере и сверка в
`AudioQueue` на клиенте — чанк мог уйти в сокет раньше, чем пришла отмена.

Подробнее — `ARCHITECTURE.md` и `docs/`.

## Реализованные компоненты

Проверено по коду.

- [x] Пять сервисов, `docker compose up`, health-check без ключей API
- [x] Общий пакет контрактов (`packages/contracts`) как единственный источник истины
- [x] Рендер персонажа в браузере: TalkingHead + HeadAudio, виземы из AudioWorklet
- [x] Диалог с сохранением состояния сессии на сервере
- [x] Стриминг: токены режутся `SentenceSplitter` по границе предложения и уходят в TTS
- [x] Прерывание — локальная остановка без обращения к сети (`cancelPlayback.ts`)
- [x] `gen_id`: инкремент до отмены задач, отмена дожидается фактической остановки,
      сверка на сервере (`is_stale`) и независимо на клиенте (`useSessionSocket.ts`)
- [x] Конечный автомат этапов с принудительным переходом по `max_turns` и пометкой
      в истории — методист видит разницу между «прошёл» и «дожали таймаутом»
- [x] Субтитры с таймингами от начала аудио поколения
- [x] Отчёт: проверка дословности цитат, проверка покрытия рубрики,
      средневзвешенный балл считается кодом, а не берётся из ответа модели
- [x] Экраны обеих ролей: список сценариев, сессия сотрудника, отчёт методиста
- [x] Админ-панель: спаны конвейера, Gantt по ходам, агрегаты нагрузки с p95
- [x] Офлайн-разбор сессии — `scripts/session_gantt.py`
- [x] 200+ тестов (99 gateway + 62 ai-service + 50 frontend, проверено
      06.09.2026; scenario-service/speech-service/gigaam-worker не пересчитаны),
      работают без сети и без Docker
- [x] Провайдеры: LLM — Anthropic, OpenAI-совместимый прокси и локальный Ollama;
      TTS — Soniox; STT — Soniox с failover на локальный GigaAM; у всех `mock`
- [x] Два готовых сценария: собеседование джуна (3 этапа), возражение по цене (4 этапа)
- [x] Редактор сценария в интерфейсе (`ScenarioEditor.tsx`): методист правит
      персону, этапы и рубрику без API, есть ИИ-черновик по короткому брифу
- [x] Голосовой ввод: захват микрофона, push-to-talk, потоковый STT,
      автоматический failover провайдера в рамках одного хода
- [x] Сжатие вытесненной истории для окна контекста — реальный вызов быстрой
      модели, инкрементальный (не пересобирает всю историю на каждый ход)
- [x] Отчёт переживает первый неудачный ответ модели: один корректирующий
      повтор с указанием конкретной причины отказа, и ручной пересчёт
      отчёта методистом отдельной кнопкой, если модель не справилась дважды

**Реализовано частично**

- [~] Конвейер последовательный: TTS предложения N не идёт параллельно чтению
      токенов N+1, что съедает time to first audio на длинных ответах
- [~] Клиентский VAD для барж-ина без удержания кнопки не реализован — сейчас
      барж-ин голосом это push-to-talk с локальным детектором тишины, не
      мгновенный onset-VAD

**Сознательно отложено:** клиентский onset-VAD для мгновенного барж-ина без
удержания кнопки, фотореализм, аутентификация.

## Предварительные результаты

**Расчётный бюджет ответа** (текстовая фаза): первый токен LLM 300–800 мс +
первый чанк TTS 150–400 мс + сеть и буфер клиента 100–200 мс ≈ **0.55–1.4 с**
при пороге 3 с.

Это сумма типичных значений по компонентам, а не измеренный p95: хвост
распределения у стороннего LLM-прокси живёт своей жизнью. Плюс TTS стартует не
по первому токену, а по первому завершённому предложению — на длинном первом
предложении оценка сдвигается вверх. Обе поправки снимаются только замером.

**Расчётный бюджет перебивания:** обработчик < 5 мс + `cancelPlayback()`
< 20 мс = **< 25 мс до тишины** при пороге 300 мс. Всё локально, сети нет.

| № | Метрика | Порог | Цель | Замер |
|---|---|---|---|---|
| 1 | Time to first audio, p95 | ≤ 3.0 с | ≤ 1.5 с | частично: первый токен LLM измерен (p50 0.57 с, p95 1.85 с на 15 синтетических тренировках, `PRODUCT_demo_technical_metrics.md`), TTS-хвост до звука отдельно не изолирован |
| 2 | Смещение аудио и губ | ≤ 200 мс | −45…+125 мс | не проведён; см. оговорку про outputLatency в «Ограничениях» |
| 3 | Остановка при перебивании | ≤ 300 мс | ≤ 150 мс | не проведён |
| 4 | Возвраты отменённого хвоста | 0 | 0 | тест есть, ручной прогон не проведён |
| 5 | Корректно завершённые сценарии | ≥ 4 из 5 | 5 из 5 | 15 из 15 синтетических тренировок получили отчёт (`PRODUCT_demo_technical_metrics.md`); живой прогон с человеком не проведён |
| 6 | Себестоимость минуты | ≤ 10 ₽ — наша цель, в кейсе такой метрики нет | — | посчитана: см. `PRODUCT_hypotheses.md`, Г5 (расчётно, 1,4–1,94 ₽/мин) и раздел «Переход на локальную модель» выше (живой 5-минутный прогон) |

**Честный статус:** классификация и итоговая оценка теперь логируют
`llm.usage` (prompt/completion tokens) на каждый вызов — этого хватило для
расчёта себестоимости выше без изменений в остальном коде. Метрики 2–4
по-прежнему не считаются системой автоматически: таблица `spans` содержит
сырой материал, но требует ручного запроса к БД. Сам замер на чужой сети и
демонстрационном звуковом тракте по-прежнему запланирован до финала.

## Ограничения

**Фонетика.** Модель HeadAudio (`model-en-mixed.bin`) обучена на английской
фонетике. VAD относительно языко-агностичен, но точность визем на русской речи
не проверялась.

**Миграции.** Схема поднимается через `create_all`, который создаёт
отсутствующие таблицы, но не меняет существующие. Дважды за итерацию
потребовался ручной `ALTER TABLE`. Первая же следующая правка схемы должна идти
через Alembic.

**Один воркер uvicorn.** `GenerationRegistry` живёт в памяти процесса; второй
воркер разъедет `gen_id` и сломает инвариант отмены. Лечение — Redis, но при
одной сессии это лишний контейнер.

**Задержка вывода не компенсируется.** Виземы берутся из AudioWorklet, то есть
из сэмплов на входе в аудиограф, а физический вывод отстаёт на
`AudioContext.outputLatency` — от 20–40 мс на встроенных динамиках до сотен
миллисекунд на Bluetooth. Губы при этом идут впереди звука, а допуск на
опережение вдвое строже допуска на отставание. Замер обязателен именно на том
звуковом тракте, на котором пойдёт демонстрация.

**Качество ИИ-части не измерено.** Точность классификатора
`complete / incomplete / off_topic`, от которой зависит корректное прохождение
сценария, нигде не считалась. Согласованность оценок модели с оценками человека
тоже.

**Себестоимость посчитана для собственной инфраструктуры, не для облачных
LLM-провайдеров.** Provider'ы теперь логируют usage-поля на каждый вызов, и
на локальном Ollama себестоимость токена нулевая сверх амортизации железа
(см. «Переход на локальную модель»). Пересчёт на конкретного облачного
провайдера (Anthropic, OpenAI-совместимый) при иной конфигурации отдельно
не проводился.

**Конвейер последовательный.** `tts_synthesize` вложен в `character_reply` без
реального параллелизма.

**Аудио сотрудника не хранится.** Колонка `audio_ref` заведена, но в текстовой
фазе писать нечего; понадобится с голосом вместе с объектным хранилищем и
retention.

**Окно контекста сжимается инкрементально, не полным пересчётом.** Реализовано;
раньше в этом разделе стояло «не сжимается вообще» — это устарело.

**Заглушки.** `TTS_PROVIDER=elevenlabs|yandex` остаются заглушками; реализован
Soniox.

## Открытые вопросы

1. Насколько точны виземы HeadAudio на русской речи — нужен замер, протокол
   проверки вендора к HeadAudio не применялся.
2. Согласится ли методист с оценкой ИИ без правок — главная продуктовая
   гипотеза, требует живого методиста и разбора отчётов.
3. Укладывается ли себестоимость минуты в 10 ₽ — расчёт не проводился.
4. Насколько точен классификатор ответов — от него напрямую зависит метрика
   корректного прохождения сценария.
5. Где хранить аудио-сегменты при переходе на голос — файлы на томе рядом с БД
   или BLOB-колонка; формат `audio_ref` не зафиксирован.
6. ~~Какой STT-провайдер выбирать~~ — решено: Soniox основной, локальный
   GigaAM — автоматический failover в рамках одного хода.

## План работ до финальной защиты

**Приоритет 1 — приёмка.**
- Скрипт автопрогона пяти диалогов с печатью таблицы метрик; переиспользует
  существующие спаны и `/admin/load`
- Замер всех метрик на чужой сети и на том звуковом тракте, через который пойдёт
  демонстрация; внесение чисел в таблицу
- ~~Повтор запроса к модели при отбраковке отчёта~~ — сделано: один
  корректирующий повтор с указанием причины отказа, плюс ручной пересчёт
  методистом отдельной кнопкой
- Оценка точности классификатора на размеченных ответах
- ~~Подсчёт себестоимости минуты~~ — сделано, см. «Переход на локальную модель»

**Приоритет 2 — демонстрация.**
- Панель метрик в интерфейсе: TTFA последнего ответа, время остановки, счётчик
  отброшенных генераций — судья видит числа своими глазами
- Сценарий демонстрации с заложенным моментом перебивания на середине длинной
  реплики
- Fallback-TTS на случай отказа внешнего провайдера во время выступления

**Приоритет 3 — устойчивость.**
- Guardrails агента: попытки увести персонажа со сценария, спросить правильный
  ответ, молчание, ответ не по теме
- ~~Валидация JSON-ответа модели с повтором при поломке формата~~ — сделано:
  `response_format=json_schema` вместо мягкого `json_object`, с фолбэком

**Приоритет 4 — если останется время.**
- Уровни сложности собеседника: лояльный, скептик, агрессивный
- Сравнение попыток одного сотрудника по критериям
- Проверка визем на русской фонетике

## Команда и распределение задач

Три человека в продукте (один AI Product и два AI Engineer) плюс DevOps на
инфраструктуре и Claude Code как рабочий инструмент всех троих, не только
Райяна — это стоит проговорить явно, а не прятать за формулировкой
«ручной код». Выделенных фронтендера, бэкендера и дизайнера нет — вся
инженерная работа распределена между двумя инженерами.

Это определило приоритеты: мы вложились в архитектурные инварианты, которые
защищают приёмку без ручного контроля (запрет таймеров на уровне линтера,
проверка цитаты и пересчёт балла в коде, двойная сверка `gen_id`), и
сознательно отказались от всего, что требует отдельных рук — фотореализма,
кастомного интерфейса.

| Участник | GitHub | Роль | Зона ответственности | Выполненные задачи | Коммитов |
|---|---|---|---|---|---:|
| Райян | `rayyanus`, `qamaletdinov` | AI Product | Продуктовая постановка, гипотезы, границы MVP, приёмочные критерии | Сформулированы гипотезы и порядок их проверки, определивший порядок разработки; заданы границы MVP и обоснованы отказы (фотореализм); продуктовые требования доведены до инвариантов кода; собран и актуализирован комплект продуктовых материалов, включая разбор и подтверждение себестоимости минуты на реальной модели | 21 + 1 merge |
| Люся | `tari-lucy` | AI Engineer | Браузерный контур: 3D-аватар, липсинк, голосовой ввод, аудио-конвейер | Интеграция TalkingHead + HeadAudio, виземы из AudioWorklet вместо поллинга; `PlaybackClock` и `AudioQueue`; локальная отмена воспроизведения; субтитры с таймингами от начала поколения; захват микрофона и STT-конвейер (`speech-service`, `gigaam-worker`) | 20 |
| Даня | `TanellD` | AI Engineer | Серверный контур: gateway, оркестратор, провайдеры, `poc` | Конечный автомат этапов, `GenerationRegistry` и протокол отмены, конвейер стриминга с нарезкой по предложениям, интеграция LLM и TTS, сборка отчёта с проверками, админ-панель со спанами; рабочий спайк в ветке `poc`, подтвердивший выбор рендера | 45 |
| Гермес | — | DevOps | Инфраструктура вне этого репозитория: Minisforum (Ryzen 9950X, 96 ГБ RAM, Proxmox, MXL-контейнер) — общие сервисы; личный ML-сервер Райяна (RTX 5090, Ryzen 9950X, 128 ГБ RAM) — хостинг локальных LLM (см. «Переход на локальную модель»), Prometheus + Grafana для мониторинга | Развёртывание и сопровождение серверов, на которых крутится локальная модель и общая инфраструктура; в git-историю этого репозитория не попадает — работа вне кодовой базы | — |

Числа — по `git shortlog -sn` на момент 06.09.2026, а не оценка на глаз.
Ранее здесь стояло «инженерная часть 50/50, браузерный контур за Люсей,
серверный за Даней» — по факту коммитов оба трогают весь стек (`frontend`,
`gateway`, `speech-service` у обоих в top-3 по числу файлов), архитектурная
зона ответственности из таблицы отражает то, ЧЬИ решения были ключевыми в
каждой области, а не то, что один человек не касался кода другого.

**Про долю Claude Code.** Инструментом пользуются все трое, не только
Райян: из 88 коммитов в репозитории **48 несут `Co-Authored-By: Claude`**
(Даня — 23 из 45 своих, Люся — 4 из 20, Райян — 20 из 21), ещё один коммит
(мёрдж) сделан от имени самого агента. Формулировка «ручной код» в этой
таблице означает «без явного участия Claude Code в конкретном коммите» —
у Райяна таким остался один коммит (`232a9bf`, «frontend loaded»), у
остальных двоих таких большинство, но не все.
